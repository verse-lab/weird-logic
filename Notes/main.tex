\documentclass[12pt]{article}

% Packages
\usepackage[utf8]{inputenc}
\usepackage{amsmath, amssymb}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{ dsfont }
\usepackage{xcolor} % for color customization

\lstset{
  basicstyle=\ttfamily\small,
  backgroundcolor=\color{gray!10},
  frame=single,
  breaklines=true
}

\newcommand{\hspec}[1]{{\color{grown}\set{#1}}}
\newcommand{\precond}[1]{\left{#1\right}}
\newcommand{\hmid}[1]{\left[#1\right]}
\newcommand{\hdummy}{\hspec{\!\!\!\!\begin{array}{c}\phantom{a}\\[2pt]\phantom{a}\end{array}\!\!\!\! \!}}

% Title
\title{Weird Logic}
\date{\today}

\begin{document}

\maketitle

\section{Examples}
The hyper triple:
\[
\{ \top \} 
\left[ \begin{array}{l} p \in \mathcal{P} : C(p) \\ l \in L : l\end{array} \right] 
\left\{ \begin{array}{l} V_C \\ V_L \end{array}
\ \middle|\
\begin{array}{l}
~ \forall ~l \in L, \exists ~p \in \mathcal{P} \\
~ Q(p,l,V_C(p), V_L(l)) \end{array}\right\}
\]

\subsection{\texttt{if-else}}
Given a Doppler attack language $L$:

\[
\begin{array}{rrl}
     L & \rightarrow & ~~S_1 : x=x+1 \\
       &   & |~S_2 : x=x+2 \\
\end{array}
\]

and vulnerable program $C$:
\begin{lstlisting}
choose a
if a then
    x = x+1
else
    x = x+2
\end{lstlisting}

write the triple: 
\[
\left\{ \forall l, p, x(C(p)) = x(l) \right\}
\left[
\begin{array}{ll}
p \in \mathcal{P} & , ~C(p) \\
l \in L & , ~l
\end{array}
\right]
\left\{
\begin{array}{c}
\forall l \in L , \exists p \in \mathcal{P} \\
x(C(p)) = x(l)
\end{array}
\right\}
\]

where $P = \{a_\top, a_{\bot} \}$,

\[
\left\{ \forall l, p, x(C(p)) = x(l) \right\}
\left[
\begin{array}{ll}
p \in \{a_\top, a_{\bot}\} & , ~C(p) \\
l \in S_1 \mid S_2 & , ~l
\end{array}
\right]
\left\{
\begin{array}{c}
\forall l \in L, \exists p \in \mathcal{P} \\
x(C(p)) = x(l)
\end{array}
\right\}
\]

Apply GrmDisj-rule:


\[
\quad
\frac{
\begin{array}{c}
L = L_1 \mid L_2 \\
\{ P \} \ [C(\mathcal{P}) ; L_1] \ \{ Q\} ~~~~
\{ P \} \ [C(\mathcal{P}) ; L_2] \ \{ Q\} \\
\end{array}
}{
\{ P \} \ [ C(\mathcal{P}); L] \ \{ Q \}
}
\]

Partition $S_1$ and $S_2$:

\[
\forall i \in \{1, 2\}, 
\left\{  \forall l, p, x(C(p)) = x(l) \right\}
\left[
\begin{array}{ll}
p \in \{a_\top, a_\bot\} & , ~C(p) \\
l \in S_i & , ~l
\end{array}
\right]
\left\{
\begin{array}{c}
\forall l \in S_i, \exists p \in \mathcal{P} \\
x(C(p)) = x(l)
\end{array}
\right\}
\]

Apply Weaken-rule:
\[
\quad
\frac{
\begin{array}{c}
 \mathcal{P} \supseteq \mathcal{P}_1 ~~~~
\{ P \} \ [C(\mathcal{P}_1) ; L_2] \ \{ Q\} \\
\end{array}
}{
\{ P \} \ [ C(\mathcal{P}); L] \ \{ Q \}
}
\]

Let $\mathcal{P}_1 = \{a_\top\}; \mathcal{P}_2 = \{a_\bot\}$, shrink $\mathcal{P}$:

\[
\forall i \in \{1, 2\}, 
\left\{  \forall l, p, x(C(p)) = x(l) \right\}
\left[
\begin{array}{ll}
p \in  \mathcal{P}_i& , ~C(p) \\
l \in S_i & , ~l
\end{array}
\right]
\left\{
\begin{array}{c}
\forall l \in S_i, \exists p \in \mathcal{P}_i \\
x(C(p)) = x(l)
\end{array}
\right\}
\]

Apply XXX-rule:

\[
\quad
\frac{
\begin{array}{c}
 \exists s , L = \{s\}~~~~
\exists p \in \mathcal{P},~\{ P \} \ [C(p) ; s] \ \{ Q(s,p)\} \\
\end{array}
}{
\{ P \} \ [ C(\mathcal{P}); L] \ \{ \forall l \in L, \exists p \in \mathcal{P}, Q(l,p) \}
}
\]


\subsection{\texttt{choose-while}}
Given a Doppler attack language $L$:

\[
\begin{array}{rrl}
     L & \rightarrow & ~~S_1 \\
     S_1 & \rightarrow & ~~x=x+1\\
      &  & \mid S_1;S_1
\end{array}
\]

and vulnerable program $C$:
\begin{lstlisting}
choose b
while b>0 do
    b--
    x = x+1
\end{lstlisting}

write the triple: 
\[
\left\{ \forall l, p, x(C(p)) = x(l) \right\}
\left[
\begin{array}{ll}
p \in \mathcal{P} & , ~C(p) \\
l \in L & , ~l
\end{array}
\right]
\left\{
\begin{array}{c}
\forall l \in L , \exists p \in \mathcal{P} \\
x(C(p)) = x(l)
\end{array}
\right\}
\]

Apply GrmDisj-rule:
\[
(1)~
\left\{ \forall l, p, x(C(p)) = x(l) \right\}
\left[
\begin{array}{ll}
p \in \mathcal{P} & , ~C(p) \\
l \in \{x=x+1\} & , ~l
\end{array}
\right]
\left\{
\begin{array}{c}
\forall l \in L , \exists p \in \mathcal{P} \\
x(C(p)) = x(l)
\end{array}
\right\}
\]

\[
(2)~
\left\{ \forall l, p, x(C(p)) = x(l) \right\}
\left[
\begin{array}{ll}
p \in \mathcal{P} & , ~C(p) \\
l \in \{S_1;S_1\} & , ~l
\end{array}
\right]
\left\{
\begin{array}{c}
\forall l \in L , \exists p \in \mathcal{P} \\
x(C(p)) = x(l)
\end{array}
\right\}
\]

(1) is similar to \texttt{if-else} example, prove (2):

Apply Weaken-rule to (2):
\[
\left\{ \forall l, p, x(C(p)) = x(l) \right\}
\left[
\begin{array}{ll}
p \in \{b \mid b \geq 2 \} & , ~while ~p>0~do \\
& ~~~p-- ;x=x+1 \\
l \in \{S_1;S_1\} & , ~l
\end{array}
\right]
\left\{
\begin{array}{c}
\forall l \in L , \exists p \in \mathcal{P} \\
x(C(p)) = x(l)
\end{array}
\right\}
\]



\subsection{\texttt{while-choose}}
Given a Doppler attack language $L$:

\[
\begin{array}{rrl}
     L & \rightarrow & ~~S_1 \\
     S_1 & \rightarrow & ~~x=x+1 \\
     &  & \mid S_1;S_1
\end{array}
\]

and vulnerable program $C$:
\begin{lstlisting}
while a>0 do
    a--
    choose a
    x = x+1
\end{lstlisting}

or vulnerable program $C'$:
\begin{lstlisting}
while b do
    choose b
    x = x+1
\end{lstlisting}

write the triple for $C'$: 
\[
\left\{ \forall l, p, x(C(p)) = x(l) \right\}
\left[
\begin{array}{ll}
p \in \mathcal{P} & , ~C(p) \\
l \in L & , ~l
\end{array}
\right]
\left\{
\begin{array}{c}
\forall l \in L , \exists p \in \mathcal{P} \\
x(C(p)) = x(l)
\end{array}
\right\}
\]

where $\mathcal{P} := \mathbb{B}^*$

Apply while-rule (wrong):
\[
\quad
\frac{
\begin{array}{c}
 p = \{p_\top \cup p_\bot\},~\mathcal{P} = p \cdot \mathcal{P}, ~~L = s;L \\
\{ P \wedge p=\{p_\top \cdot p_\bot \}\} \ [C(p) ; s] \ \{ P\} \\
\end{array}
}{
\{ P \} \ [ C(\mathcal{P}); \{L\}] \ \{ P \wedge \mathcal{P} = \mathcal{P} \cdot \{p_\bot\}\}
}
{\text{While}}
\]

$Q': \forall l \in L , \mathcal{P} =\mathcal{P} \cdot \{ p_\bot\} \wedge x(C(p)) = x(l) $

$\implies Q: \forall l \in L , \exists p \in \mathcal{P}, x(C(p)) = x(l)$ 

get:

\[
\left\{ \forall l, \mathcal{P} =\{p_\top \cdot p_\bot\}\wedge x(C(p)) = x(l) \right\}
\left[
\begin{array}{ll}
p \in \mathcal{P} & , ~C(p) \\
l \in \{x=x+1\} & , ~l
\end{array}
\right]
\left\{
\begin{array}{c}
\forall l \in L , p \in \mathcal{P} \\
x(C(p)) = x(l)
\end{array}
\right\}
\]

Apply XXX-rule:

\[ 
\left\{ x(C(p)) = x(l) \right\}
\left[
\begin{array}{ll}
C(p)\\
x=x+1
\end{array}
\right]
\left\{
\begin{array}{c}
x(C(p)) = x(l)
\end{array}
\right\}
\]

where $p=\{p_\top \cdot p_\bot\}$

\end{document}

\bibliographystyle{plain}  % or another style like ieeetr, acm, etc.
\bibliography{references}